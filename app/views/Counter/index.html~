#{extends 'main.html' /}
#{set title:'Home' /}

<h1>Counting..</h1>

#{if cl}
    #{if cl.citems}
        
        <ul id="fakeTable">
            <li> 
                <li class="colheading">Denominación</li>
                <li class="colheading">#</li>
                <li class="colheading">Suma</li>
            </li>

            #{list items:cl.citems, as:'citem'}
            <li> 
                <li class="numvalue">${citem.currency.convRate}</li>
                <li class="numvalue">${citem.quantity}</li>
                <li class="numvalue">${citem.value()}</li>
            </li>
            #{/list}

            <li> 
                <li>Total</li>
                <li>..</li>
                <li class="numvalue">${countResult}</li>
            </li>
        </ul>
    #{/if}
    #{else}
        No items counted.
    #{/else}    

#{/if}
#{else}
    No count data.
#{/else}    

